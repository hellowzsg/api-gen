syntax = "proto3";

package simple.config;

option go_package = "github.com/acme/simple-config/generated/go/simple/config;config";

// ConfigId is the primary key for the Config entity.
message ConfigId {
  string key = 1;
}

// ConfigEntry is a simple key-value configuration resource.
message ConfigEntry {
  string value = 1;
}
