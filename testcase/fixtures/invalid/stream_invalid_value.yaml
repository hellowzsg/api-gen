# Invalid custom_methods[].stream value — must be "", "server", "client", or "bidi"
syntax: v1
name: test.invalid
import_protos:
  - path: "proto/**/*.proto"
settings:
  go_repo: github.com/test/repo
  out:
    proto: generated/proto
    go: generated/go
entities:
  - name: foo
    key:
      type_: FooId
    resources:
      - name: bar
        type_: Bar
        version: { kind: NONE }
        reader: {}
services:
  - name: FooService
    entities:
      - name: foo
    custom_methods:
      - name: CustomOp
        request: Meta
        response: Bar
        stream: both
