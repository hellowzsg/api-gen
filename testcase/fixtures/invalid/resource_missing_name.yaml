# A8: resource missing name
syntax: v1
name: test.resource_missing_name
import_protos:
  - path: "proto/**/*.proto"
entities:
  - name: foo
    key:
      type_: FooId
    resources:
      - type_: Bar
        version: { kind: NONE }
services:
  - name: FooService
    entities:
      - name: foo
