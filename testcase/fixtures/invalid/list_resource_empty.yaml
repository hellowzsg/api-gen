# E5: entity.list.resource is an empty list — must fail-fast
syntax: v1
name: test.list_resource_empty
import_protos:
  - path: "proto/**/*.proto"
entities:
  - name: foo
    key:
      type_: FooId
    list:
      resource: []
    resources:
      - name: bar
        type_: Bar
        version: { kind: NONE }
services:
  - name: FooService
    entities:
      - name: foo
