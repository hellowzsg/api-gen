# B2: key.type_ starts with dot (invalid type reference)
syntax: v1
name: test.key_type_dot_prefix
import_protos:
  - path: "proto/**/*.proto"
entities:
  - name: foo
    key:
      type_: ".FooId"
    resources:
      - name: bar
        type_: Bar
        version: { kind: NONE }
services:
  - name: FooService
    entities:
      - name: foo
