# Fixture for api.lock corruption test
# This fixture is valid YAML, but the api.lock file in the same directory is corrupt.
syntax: v1
name: test.corrupt_lock
import_protos:
  - path: "proto/**/*.proto"
settings:
  go_repo: github.com/test/repo
  out:
    proto: generated/proto
    go: generated/go
entities:
  - name: foo
    key:
      type_: FooId
    resources:
      - name: bar
        type_: Bar
        version: { kind: NONE }
        reader: {}
services:
  - name: FooService
    entities:
      - name: foo
