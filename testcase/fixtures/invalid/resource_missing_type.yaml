# A9: resource missing type_
syntax: v1
name: test.resource_missing_type
import_protos:
  - path: "proto/**/*.proto"
entities:
  - name: foo
    key:
      type_: FooId
    resources:
      - name: bar
        version: { kind: NONE }
services:
  - name: FooService
    entities:
      - name: foo
