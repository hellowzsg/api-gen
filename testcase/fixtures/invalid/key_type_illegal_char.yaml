# B4: key.type_ contains illegal character
syntax: v1
name: test.key_type_illegal_char
import_protos:
  - path: "proto/**/*.proto"
entities:
  - name: foo
    key:
      type_: "Foo-Id"
    resources:
      - name: bar
        type_: Bar
        version: { kind: NONE }
services:
  - name: FooService
    entities:
      - name: foo
